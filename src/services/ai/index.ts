/**
 * AI tutor / AI explanation service placeholder.
 *
 * TODO Phase 3:
 * - generateExplanation(questionId): produce an AI-written explanation
 *   using the Anthropic API, cached against Question.explanation.
 * - chatWithTutor(userId, message, context): conversational tutor scoped
 *   to a paper/topic.
 */

export async function generateExplanation(_questionId: string): Promise<string> {
  throw new Error("AI explanations are not implemented in Phase 1.");
}
